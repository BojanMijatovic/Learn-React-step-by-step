import styles from '../style/layout/_header.module.scss';

const Header = () => {
  return (
    <div className={styles.header}>
      <h1>Covid Tracker</h1>
    </div>
  );
};

export default Header;
